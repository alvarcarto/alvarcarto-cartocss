
deployment:
  production:
    branch: master
    commands:
      - npm install
      - carto styles/bw/project.mml > styles/bw.xml
      - carto styles/minimal/project.mml > styles/minimal.xml
      - scp -r styles/*.xml alvar@tile-api-qa.alvarcarto.com:/mapnik-styles
      - ssh alvar@tile-api-qa.alvarcarto.com "pm2 restart all"
